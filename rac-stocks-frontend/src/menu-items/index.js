// project import
import general from './general';
import stocks from "./stocks"

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [general, stocks]
};

export default menuItems;
